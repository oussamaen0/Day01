#include <stdio.h>
#include <string.h>

int main()
{
    char c[100];
    char d[100];
        printf("entre your chaine caracter : ");
        gets(c);
         printf("entre your chaine caracter : ");
        gets(d);
        printf("vous avez entre : %s %s", c,d);

    return 0;
}